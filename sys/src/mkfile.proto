#
# common mkfile parameters shared by all architectures
#

# include i for mk clean
OS=568ijv
# omit arm64 until 7[cal] can build everything
CPUS=amd64 arm 386 riscv64 mips
CFLAGS=-FTVw
LEX=lex
YACC=yacc
MK=/bin/mk

# recursive mk will have these set from the parent
# this is never what we want.  clear them

TARG=
OFILES=
HFILES=
YFILES=
