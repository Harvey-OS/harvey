How to build:

1. Make sure you have Go and Plan 9 Ports installed
2. Run "sh BUILDTOOLCHAIN"
3. Run ". SETUP"
4. cd sys/src; mk libs; cd cmd; mk install # this gets the libraries and commands
5. cd $NXM/sys/src/nix/k10; mk # build the kernel
