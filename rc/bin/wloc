#!/bin/rc
oifs=ifs
ifs='
'

for(i in `{ls /dev/wsys}) {
	echo -n window -r `{syscall -o read 0 buf 59 < $i/window >[2] /dev/null |
	sed 's/............//; s/^ *//; s/  */ /g'}
	echo -n ' '
	cat $i/label
	echo
}
